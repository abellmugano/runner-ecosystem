# Build System - Phase 2 (Expanded)

- Objetivos: suporte a múltiplos projetos, cache, plugins (pré/pós-build).
- Estrutura descrita na tarefa: config.json ampliado, build/clean/cache, etc.
- Como usar com orbit:
  - orbit build build -ProjectName <nome> [-Target <debug|release>]
  - orbit build clean
  - orbit build cache --status
